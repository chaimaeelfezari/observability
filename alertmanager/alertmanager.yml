global:
  resolve_timeout: 1m

route:
  receiver: discord
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 1h

receivers:
  - name: discord
    webhook_configs:
      - url: 'http://discord-proxy:5001/webhook'
        send_resolved: true